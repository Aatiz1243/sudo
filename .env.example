# ── Sudo Bot — Environment Variables ─────────────────────────────────────────
# Copy this file to .env and fill in your values. Never commit .env to git.

# Discord Bot Token (required)
# Get it from: https://discord.com/developers/applications → your app → Bot
BOT_TOKEN=your_discord_bot_token_here

# Discord Application/Client ID (required for `npm run deploy`)
# From the same Developer Portal page → General Information → Application ID.
# Needed once to register slash commands; the bot itself doesn't need it to run.
CLIENT_ID=

# Guild ID for instant slash-command deploy during development (optional)
# Right-click your test server icon → Copy Server ID. Leave blank to deploy
# globally instead (works everywhere, but can take up to an hour to appear).
GUILD_ID=

# Your Discord User ID (recommended)
# Enables the !approve command so you can add new responses live, without restarting.
# How to get: Settings → Advanced → Developer Mode ON → right-click your name → Copy User ID
OWNER_ID=

# Channel ID for weekly unknown command logs (optional)
# Every Sunday the bot posts the top unknown $sudo commands here for review.
LOG_CHANNEL_ID=
